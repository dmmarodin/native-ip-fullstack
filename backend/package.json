{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "src/server.js",
  "scripts": {
    "migrate": "NODE_ENV=development ./node_modules/.bin/wait-port -t 50000 mysql:3306 && npx sequelize db:migrate --migrations-path './src/migrations' && npx sequelize db:seed:undo:all --seeders-path './src/seeders' && npx sequelize db:seed:all --seeders-path './src/seeders'",
    "start": "npm run migrate && nodemon --legacy-watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mysql2": "^2.2.5",
    "sequelize": "^6.6.5",
    "sequelize-cli": "^6.2.0",
    "socket.io": "^4.1.3",
    "wait-port": "^0.2.9"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  }
}
